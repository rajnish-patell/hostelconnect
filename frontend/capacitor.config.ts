import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.vercel.hostelvideocall',
  appName: 'Hostel Video Call',
  webDir: 'dist'
};

export default config;
